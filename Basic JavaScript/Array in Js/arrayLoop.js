 /**
   * using loop to print arrray (2)
   */

  var ArrLoop = [1,3,6,93,2];
  for (var j = 0 ; j<ArrLoop.length; j++){
    console.log(ArrLoop[j]);
  }

  /**
   * sum loop
   */
//define zero variable to sum the total values at the end
 let sumLoop = 0;
 for (let i=0; i<=10;i++){// 
   sumLoop += i; //equal to sumloop = sumloop + i or sumloop = 0 + total values
 }
 console.log(sumLoop);