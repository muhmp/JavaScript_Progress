/** 
function printArray(number) {
    var newArray = [];
  
    for(var i = 1; i <= number; i ++) {
      newArray.push(i);
    }
  
    return newArray;
  }
  console.log(printArray(10));
*/
  

//again
function num(N){
  //define array
  let number = []; //to push the value to the array
  //to the loop
  for(let i=0; i <N.length; i++){
    number.push(i);
  }
  //return 
  return number;
}
console.log()