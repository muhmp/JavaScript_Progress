/**shift method
 * The shift() method removes the first element
 * from an array and returns that removed element. 
 */

const arr1 = [1,2,3];

const firstElement = arr1.shift(); //remove the first

console.log(arr1);