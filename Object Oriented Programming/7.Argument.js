
/**
 * Extend Constructors to Receive Arguments
 * @param {*} name 
 * @param {*} color
 * Using argument 
 */
function Dog(name, color) {
    this.name = name;
    this.color = color;
    this.numLegs = 4;
  }
  //code here
  let terrier = new Dog();